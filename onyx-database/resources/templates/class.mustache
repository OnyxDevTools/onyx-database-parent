package {{packageName}};

import com.onyx.persistence.annotations.*;
import com.onyx.persistence.*;

@Entity
public class {{className}} extends ManagedEntity implements IManagedEntity
{
    public {{className}}()
    {

    }

    @Attribute
    @Identifier(generator = {{{generatorType}}}, loadFactor = {{{idLoadFactor}}})
    public {{{idType}}} {{idName}};

{{#attributes}}

    {{#isPartition}}
    @Partition
    {{/isPartition}}
    {{#isIndex}}
    @Index(loadFactor = {{{loadFactor}}})
    {{/isIndex}}
    @Attribute
    public {{{type}}} {{name}};

{{/attributes}}

{{#relationships}}

    @Relationship(type = {{{relationshipType}}},
                  inverseClass = {{{inverseClass}}}.class,
                  inverse = "{{{inverse}}}",
                  fetchPolicy = {{{fetchPolicy}}},
                  cascadePolicy = {{{cascadePolicy}}},
                  loadFactor = {{{loadFactor}}})
    public {{{type}}} {{{name}}};

{{/relationships}}

}
